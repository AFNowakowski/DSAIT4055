"""Project-level constants for the NLP pipeline."""

TEXT_COLUMNS = ["question_title", "question_body", "answer_body"]
ID_COLUMNS = ["question_id", "answer_id"]
TIMESTAMP_COLUMNS = ["accepted_at", "event_at"]
